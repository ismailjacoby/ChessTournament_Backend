spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chess_tournament
    username: postgres
    password: 1285
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    database: postgresql


  mail:
    host: smtp.gmail.com
    port: 587
    username: checkmatemonsieur@gmail.com
    password: akxhofwwigsxrmjs
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true

server:
  port: 8080
  error:
    include-message: always

